# C185 preregistration — learned single-missing acquisition

V5-E; akakishi04/fold; branch feat/sft-target-loss.
Registered AFTER C184 acceptance08ac8ef47b917a34d54dd1d84ac1ede41b2f88d6.
**C185 ACTIVE / NOT YET JUDGED. C186 NOT REGISTERED. Gate E NOT PASSED.**
C184/C183/C181 PASS and C182 VALID NEGATIVE, and every earlier verdict, stay fixed.
No C184 rerun; no checkpoint/threshold/seed/case repair.

## One scientific question

Can the frozen C181 necessity classifiers, through the accepted C184 numeric adapter
and an explicit external-binding bridge, trigger exactly the needed real bounded
acquisitions and reclassify the actually updated state correctly?

Previously, C173 exercised real file acquisition with scripted action selection;
C181-C184 exercised learned classification without triggering that lifecycle.
C185 connects those boundaries. It is not another naming-only score diagnostic.
No learned target/tool selection, answer/proof generation or final Gate E claim.

## Changed boundary / held constant

Current TaskView -> C170 encode -> C184 canonicalize plus matching opaque bindings
-> unchanged C178 preparation -> frozen C179 TREE_LINKS base restored from C181
-> raw necessity argmax -> if NEEDS, handwritten unique-target inverse mapping
-> actual C172 reservation -> actual C173 FileSnapshotProvider / admission
-> current TaskView including consumed resources -> SAME model -> raw reclassification.

All six C181 final checkpoints are kept: seeds181001/181002/181003, FINAL_ONLY and
INTERNAL_SEMANTICS. Use C182.restore_bare, validating the complete25921parameter
fingerprint but constructing ONLY the25726parameter base. No auxiliary head or teacher
at inference. No optimizer, training, new seed, ensemble, best-checkpoint selection,
logit correction, threshold fit, or symbolic necessity solver in the execution path.
Original code/checkpoints/preregistrations are immutable. The new code is benchmark-only.

First replay all six models on the complete original9396PILOT rows, same ordering and
batch1024:56376predictions. Exact argmax and absolute logit difference<=1e-6,relative0
against saved C181 scores. Replay/source/schema/nonfinite disagreement is INVALID.
TRAIN replay is not repeated. No official new-condition scores are measured before
registration, including no use of official checkpoint outputs in helper tests.

## Fixed cohort and comparisons

Select ALL3712C174PILOT rows with exactly one UNOBSERVED fact, using visibility ONLY,
not labels, errors, margins or a preferred model. Inherited labels are768NEEDS and
2944SUFFICIENT, covering all4existing development semantic groups/116templates.
Each source row is tested in two local layouts: identity(0,1,2,3) and reverse(3,2,1,0).
Change numeric references and complete fact records together; external fact identity
travels with the record. Both possible hidden completions0/1 are tested as separate
worlds, with IDENTICAL initial visible numeric data. The completion is never a policy
input. This yields14848episodes per policy. These are reused logical tasks, not an
independent semantic holdout; layouts/worlds/models are not independent observations.

Eight policies: all six frozen models, plus MISSING_RULE and NEVER_QUERY.
MISSING_RULE requests the sole missing item regardless of the expression, then
classifies sufficient if none remain. NEVER_QUERY always classifies sufficient.
Neither rule uses correctness labels. Both use the SAME target/transport/budget
bookkeeping, while performing zero neural inference. Do not claim equal compute.
FINAL_ONLY models are descriptive learned controls; their errors remain visible and
are not required to vanish. Every policy must preserve the engineering contracts.

|Fixed quantity|Count|
|---|---:|
|Source one-missing rows|3712|
|Layouts / hidden completions / policies|2 / 2 / 8|
|Blocks|32|
|Total independent-runtime episodes|118784|
|Frozen-model episodes|89088|
|INTERNAL_SEMANTICS candidate episodes|44544|
|Rule-control episodes|29696|
|Original replay predictions|56376|
|Live initial neural predictions|89088|
|Live post-attempt neural predictions|measured,0..89088|
|Total neural predictions upper bound|234552|
|Original replay batches / cell calls|60 / 420|
|Checkpoint loads / fresh seeds / training updates|6 / 0 / 0|
|Registered tiny source files|8|

Actual subsequent inference and acquisition counts depend on RAW first decisions,
not labels. Do not force the expected correct count or fake a post-call after a skipped
acquisition. Report actual forward batches,seven shared-cell calls per batch,rows,
provider calls,real file reads,bytes,admitted facts and resource charges.
If candidates are correct, their expected calls/publications are9216 and skipped
acquisitions35328. These are expectations, not a replay-triggered script or a license
to discard errors. MISSING_RULE reads14848times,11776unnecessarily; NEVER_QUERY reads0
and misses3072needed acquisitions. Those deliberate baseline errors are not Gate E failures.

## Live state and budget contract

Initial resources are the supplied C174 fields:12internal units,4acquisition units,
internal_step7,RETRIEVE available/permitted,other tools false. C185 bounds each episode
more tightly:ONE acquisition attempt and at mostTWO classification decisions.

Every decision pays ONE scheduler bookkeeping unit BEFORE encoding current state,
using the existing trusted AcquisitionOwner.refresh hook. Thus even the first live
input has internal_remaining11/internal_step8. It is NOT silently replaced by12/7.
A normal requested acquisition then pays C172reservation1 plus C173dispatch2; the second
decision pays1. Post-acquisition model input is7internal/3acquisitions/internal_step12,
with the ACTUALLY published fact and current last_outcome. No observed value, permission,
clock, revision, remaining budget or outcome is reset to a training constant.
This introduces new runtime-resource contexts, deliberately. A finite classifier error
under them is scientific negative evidence, not a reason to clamp or conceal context.
Bookkeeping units are not elapsed CPU time or exact matrix multiply costs.
The formal cohort uses available/permitted RETRIEVE and positive initial budgets.
Denial/exhaustion/corrupt-delivery helper tests use synthetic policies; they do not
establish how the frozen learned models behave under every denial condition.

If the classifier says SUFFICIENT, do not fetch, verify or construct an answer. End with
SUFFICIENT_CLASSIFICATION and the original unknown still unknown. This is a classifier
result, not a verified logical conclusion or RuntimeState ANSWERED terminal.
If it says NEEDS, select the ONLY unknown in canonical bindings, map to the current
original slot and external fact_id, then call C172/C173. Reclassify actual resulting
state once. If NEEDS remains, finish UNRESOLVED: never perform another acquisition.
No semantic label can override a wrong initial or final classification.
No root/proof/teacher state is supplied; no ANSWER, COMPUTE, OBSERVE or ASK_USER action.

Opaque binding IDs/references move consistently with C184 numeric fields. The bridge
checks complete packet decode, inverse numeric and binding reconstruction, per-leaf
fact/reference identity, request/scope, clock/revision and live resources. A saved map
is bound to a digest of the current source view; stale mapping is rejected.
This exercises C170 Binding/PolicyInput with external IDs, but NOT every C170 status,
arbitrary entity names, multiple unresolved facts or a production memory protocol.

## Actual provider and observation boundaries

Use unchanged C173 FileSnapshotProvider and AcquisitionOwner, max_dispatches1.
Eight registered files cover four opaque external fact IDs x two possible completion
bits. Each file contains ONLY the requested fact; there is no unrelated hidden payload
sent to the model. Independent episodes have distinct request/scope IDs and owners;
fixtures are synthetic alternate worlds, not claims about a globally shared real world.
Initial three observed facts/reference IDs are trusted fixtures as in the earlier
experiments, not an external authentication capability measured here.

The driver supplies an endpoint registry, never a truth label, to run_block. Only the
numeric current input reaches FrozenPolicy. Endpoint files, source-document witnesses
and their hashes stay runtime-only. Initial bindings may carry opaque episode IDs, but
those strings are outside the model's numeric features. A source is read on an actual
provider call; files are NOT cached into a dictionary to replace reads. Setup, source
hashing and serialization do incur additional IO; zero provider calls is not a claim
of zero benchmark disk IO. File hash/evidence target/time/revision validation and
publication are the unchanged C173 implementation. No network,core EvidenceState write,
durable database,asynchronous/concurrent execution or proof verification is involved.

## Fixed deciding gate

All32blocks complete. For each INTERNAL_SEMANTICS model,every14848episode must have:
1. Correct initial raw necessity classification against inherited semantic label.
2. No acquisition when already sufficient; exactly one correct acquisition/admission
   when necessary, preserving external target,scope,source,known0/1 and other facts.
3. After a necessary successful acquisition, raw post-classification SUFFICIENT.
4. Exact resource accounting, unchanged other bindings/facts, no pending duplicate work.

Zero failed episodes is required for all3candidates. Any finite violation => ACCEPTED
VALID NEGATIVE after review; save all cases and continue the remaining independent
blocks. Source/schema/nonfinite/replay/incomplete/protection exceptions => INVALID;
restore the SAME C185 execution validity, without changing scientific conditions.
Every policy, including controls, must have zero measured contract errors. MISSING_RULE
and NEVER_QUERY must reproduce their preregistered deliberate acquisition patterns.
FINAL_ONLY wrong necessity decisions are descriptive, not a requirement to pass.
Never mask raw model errors behind provider availability, a proof checker or ground truth.

PASS supports this bounded learned-trigger/real-IO/live-reobservation integration.
It does NOT mean FOLD understands natural language, can choose among several targets,
produces answers/proofs, generalizes to larger/repeated-variable logic or passes Gate E.
C181 and C184 PASS and C182 negative remain unchanged regardless of this result.

## Outputs, guards and implementation validation

13artifacts excluding summary: plan,replay,episode-results,compressed JSONL traces,
NPZ predictions plus8source files. Save all episodes' initial/final complete numeric
packets and opaque bindings,each decision's live canonical packet/map,action/dispatch/
receipt,initial/post raw decisions,scope/target and score failures. NPZ axes are
policy8 xlayout2 xcompletion2 xsource3712 xphase2 (plus feature/logit axis).
Absent decisions are-1; logit_present marks genuine neural logits. Zero-filled storage
for missing or rule-control logits is NOT a model score. Keep all initial/final mistakes.
Detailed group counts and changed-initial-decision counts are in the same batch.

Inherit C184.precheck; add accepted C184summary and5artifacts plus4own source files.
88historical source pins;182protected input paths;outer protected C37/fixture unchanged.
C184summary7817f8f17932f772d80e6a994bf58c17c8f71b73a1f5691a4a5345ce3a917e04.
Manifest SHA25661563a16d40883db8f2f651e440ae4704d5ef8f36115aca28f9bade64d3d6385.
Source-file hashes are inside the manifest before any model run. Existing artifacts,
model bytes,old code and Gate E contract are never modified. Wall-clock,IO and serialized
sizes are reported;peak RAM/VRAM and production latency are not measured or claimed.

36/36new helper tests passed using the ACTUAL new complete module and four COMPLETE
Git-blob-identical fetched C170/C171/C172/C173 modules. Local blobs verified:
structured_task_input b874b6abf17fb938a5cc873ff2090c6493f00010;
structured_derived_result56709025b02cff2dba6d7c907051a553ca5908ec;
structured_action_runtime672a79ea1c6e6467b1a5f1a474c843ce23fd1b04;
structured_acquisition_lifecycle e7fa5cbddbb650ab43b3b69683082a6edc0ac4da.
C184 local test support contains fetched adapter function definitions,NOT its full
module or full repository. Policy outputs are SYNTHETIC,not official checkpoint runs.
Tests use actual tiny files and actual reservation/admission; cover mapping all24layouts,
zero versus missing,live debits,denial,wrong delivery,failed provider,no retry,unchanged
unrelated facts,no proof fallback,unrepaired model error,raw argmax and strict gate.

No complete checkout was available (local Git DNS failed). Full1285regression,
WindowsPowerShell,the complete182input chain,original checkpoint replay and official
118784episodes remain UNEXECUTED. No official C185 scientific result is known.
Two new Python files compile;three embedded runner Python blocks parse;not PS execution.
**1285expected=1249+36;69modules.** One regression followed by one bounded batch.
Run tools/run_c185.ps1 withC184/C183/C182/C181/C180/C179/C178/C177/C176/C174summaries and
ExpectedHead. Progress:56376replay;blocks1/32..32/32;RESULT;POSTCHECK.
Judge C185 ->ledger/handoff->next design. **No C186 before judgment.**

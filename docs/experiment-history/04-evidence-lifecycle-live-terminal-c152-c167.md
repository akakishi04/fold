# Evidence lifecycle and live terminal path — C152-C167

## Scientific subject

This sequence took a selected persisted record through explicit evidence admission, state projection,
payload dereference, reobservation, learned Controller execution and typed terminal output.

## Persisted evidence lifecycle — C152-C156

### C152 — persisted retrieval bridge
PASS. 82,944 actual adapter calls across canonical/permuted layouts preserved selected identity,
provenance and payload. The source ranker's existing semantic errors remained visible.

### C153 — validated evidence admission
PASS. 82,944 requests, 580,608 delivery submissions:
- 414,720 controlled rejections
- 82,944 COMMITTED
- 82,944 DUPLICATE

Each valid logical request admitted exactly once; invalid/stale/duplicate delivery did not mutate
the prior diagnostic state.

### C154 — evidence-state projection
PASS. Request entries were projected into record references with provenance conflict rejection.

### C155 — provenance-bound payload dereference
PASS:
- 3,072 RESOLVED
- 3,072 SOURCE_UNBOUND
- 3,072 SNAPSHOT_MISMATCH
- 3,072 RECORD_UNBOUND
- 0 failed cases/state mutations

### C156 — request-bound reobservation
PASS. 82,944 requests crossed four read outcomes for 331,776 reobservation views; unresolved reads
cleared stale payload/presence instead of reusing old evidence.

## Learned Controller and live recovery — C157-C159

### C157 — learned Controller bridge
PASS. Three routers, 1,990,656 learned decisions, zero wrong actions/nonpositive margins in the
registered repeated input patterns.

### C158 — bounded Controller-driven reference recovery
PASS. 1,920 live episodes across five scenarios. The frozen Controller drove authorized retrieval,
admission/projection and live reobservation. Success reached ANSWER_ACTION; missing/denied/budget
branches stopped unresolved.

### C159 — evidence-bound typed terminal result
PASS. 1,920 base episodes and 6,528 emitter calls. Observed zero remained distinct from unresolved;
wrong request/provenance/payload and forced ungrounded answer controls were rejected.

## Composition failure and repair — C160-C163

### C160 — raw query to terminal result
**ACCEPTED VALID NEGATIVE.**
All 82,944 query-originating live episodes executed the ranking/acquisition/readback path, but:
- typed ANSWERED = 0
- failed terminal episodes = 82,944

The new evidence was a composition failure, not a regression of the individually accepted
C151-C159 components.

### C161 — failure localization
PASS. All stored cycle assessments passed; all terminal outputs were rejected at the same
MALFORMED_EVIDENCE boundary.

### C162 — evidence-container differential
PASS. Changing only the final observations container tuple->list turned all 82,944 saved native
rejections into bound outputs in the differential reconstruction.

### C163 — live evidence-container bridge
PASS. The adapted list-form evidence container restored the complete live path:
82,944 candidate ANSWERED/OBSERVED_VALUE outputs, zero failed live episodes.

## Fault branches — C164-C167

- C164 PASS: missing delivery produced bounded unresolved output with no fake publication.
- C165 PASS: permission-denied branch prevented acquisition/provider work and terminated safely.
- C166 PASS: acquisition-budget exhaustion behaved safely.
- C167 PASS: warm-reference behavior was preserved without unnecessary acquisition.

## Durable conclusions

- Selected identity, provenance, admission, reference identity, payload dereference and terminal
  output are separate contracts.
- A container/schema mismatch can break an otherwise correct end-to-end path; interface shape is
  scientifically relevant.
- Missing/denied/exhausted/warm states must be explicit state, not inferred from payload value.
- C160 remains a valid negative even though C161-C163 localized and repaired the composition path.

## Cleanup implication

C152-C167 are heavily chained. Do not delete older benchmark/helper files while current regression
lists still import them. When decoupling later, preserve the accepted interface contracts and C160
negative/repair history so the tuple/list failure is not rediscovered.

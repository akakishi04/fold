# C231 preregistration — V5-B byte evaluation contract audit

**C230 ACCEPTED PASS. C231 ACTIVE / NOT YET JUDGED. C232 NOT REGISTERED.**
Gate E PASSED; Gate F NOT PASSED. This is an existing V5-B evaluation-interface audit, not a V5-G
stage promotion or an integrated v0.5 language model claim.

## One question and fixed scope

Can the existing V5-B byte model be evaluated using prefix-only next-byte likelihood/generation,
with no target leakage, reproducible batch/singleton outputs and checkpoint replay?

No new architecture, learned-memory integration, training, external data or public benchmark.
Three freshly initialized untrained instruments are used only to validate measurement mechanics.
C230's numeric optimization track is paused; the prepared path stays optional, not default.

## Accepted parent

Scientific HEAD: `58ec6d1497e4729d394307f5c1316d9e69f2ee8e`.
Published log commit: `ad8a657842491a74d8aa5e3101893aaa98a335ca`.
Summary SHA: `3eeb739eaf37c8b0d1ef4f94d5eec46281ea8721c1cea67fb51234f8bd7fca7c`.
Validation SHA: `8f0ebfb7c769f1bc9eebceff02d51c55e1a94dd52ee0ec67b73650f2dda683a9`.
Local summary: `runs/c230-v5f-prepared-capsule-42695282c3e6402f9fe7616ba3641099/summary.json`.

Preserve the accepted221 source pins,305 protected inputs and all five parent artifacts.
Do not rerun C230 or modify its registered data/thresholds/implementation.

## Actual model under the instrument

`fold_lm/v05/language_task.py:ShortByteLanguageModel`, with fixed48 slots, width16,2 modules,
2 internal steps and hidden multiplier2. It has13488 parameters and an explicit TASK_NEXT=0 route.
The source contains a causal GRU front-end and uncompressed V5-B core. It is not the separate
legacy FoldLanguageModel, the shared-basis compressed deployment path, or the learned H1/H2 stack.
The existing prefix-boundary EOS pooling is retained and explicitly documented.

Language/import sources are pinned to these exact blobs:

| Source | Blob |
|---|---|
| fold_lm/v05/language_task.py | 587162ffc35e1d854fa8d8577740a1e034537de2 |
| fold_lm/v05/modules.py | 3413c2de62e83dbf4a98af044a5ac2b242c99f87 |
| fold_lm/data.py | 2b0c9635bc67be9a6928d0ccf65e2751719dd2f0 |
| fold_lm/__init__.py | 8977a876cd0e934ec6113d5d323e4809fd4ea1cc |
| fold_lm/v05/__init__.py | a5306fe80fd99b46a2efcce0352d29f0030e0f98 |

These cover direct and import-time repository dependencies of the language branch. Accepted C230
sources already cover its own benchmark and the historical checkpoint/validation helper graph.

## Data and intervention checks

Four authored English/Japanese UTF-8 texts, lengths16/22/16/28 bytes. Fixture SHA:
`5dcdbd8223c0e40df8d9e3fb5c98e50873a0a014ce9a3c2e003fe74d3911ce23`.

For each real target byte x[i], construct BOS+x[:i]+prefix-boundary EOS+PAD to48 slots.
Only observed bytes enter the model; the target is scorer-only. No PAD/BOS/EOS likelihood targets.
Do not decode partial UTF-8 prefixes or silently truncate them.

Compare batched/singleton logits, and original/suffix-mutated documents at positions whose observed
prefix is unchanged. Validate cross-entropy against independent logsumexp-minus-target scoring.
Greedy generation starts from the first8 bytes and adds4 model-selected bytes without feeding the
reference suffix. Save outputs as hex. Serialize/reload the untrained models and replay logits and
all generated bytes. Fingerprints must not change during evaluation.

## Workload and fixed PASS gate

Seeds231001/231002/231003; CPU float64; threads2; deterministic algorithms; training0.
Per seed126 actual model forwards, total378. Target positions246 across3 seeds; unique fixture
bytes82. These are instrument repetitions, not246 independent language test problems.

Per document:
- finite NLL/bits-per-byte;
- independent scoring error <=1e-9;
- batch/singleton error <=1e-9;
- suffix intervention error <=1e-9;
- checkpoint reload error <=1e-9;
- exact four-byte generation replay.

Every model fingerprint and serialized state must round-trip, and all call/byte counts must match.
No minimum accuracy, loss reduction or fluent generation requirement: the models are untrained.
`meaningful_language_score = False` is part of the gate. No speed/memory advantage claim.

Complete contract discrepancy is a scientific audit FAIL. Source/artifact/schema/runner defects
or incomplete execution are INVALID / RETRY SAME C231. Gate F remains NOT PASSED regardless.

## Authoring registration

OWN6:
- fold_lm/v05_benchmarks/model_c231_byte_eval_contract.py
- tests_lm/test_v05_c231_byte_eval_contract.py
- tools/run_c231.ps1
- tools/invoke_c231.ps1
- this preregistration
- docs/v5b-byte-evaluation-contract-v0.1.md

Protection uses an exact source-set contract:
`C230.source_blobs.keys union OWN6 union LM_SOURCES5`.
Overlaps are allowed only at identical registered blobs. No accepted source is overwritten.
If S is the resulting unique source count, protected inputs are305+6+(S-221). Precheck asserts the
exact set, checks every source/input, and prints both resolved counts. This avoids falsely adding
already parent-pinned LM files a second time. Tests cover the union and conflicting-parent rejection.

Artifacts5: evaluation-plan.json, byte-fixture.json, untrained-models.pt, measurements.json,
validation-summary.json. Outputs/checkpoints stay ignored under runs/; only console logs published.

New tests24; modules116; loaded2690 /focused2689. Only the inherited exact exclusion remains:
`tests_lm.test_v05_c204_live_v2_mixed_channel_loop.C204Tests.test_33_active_dispatcher_resolves_current_formal_state`.

Manifest SHA:
`745c97d3aab18c129494357cc91eb48a6e713294e437b17700be22da8bd3bf83`.

## Post-authoring review

`post_authoring_review = PENDING`

Initial targeted author-side execution:22 tests passed,0 failures/errors in1.616s using synthetic
prefix-dependent instruments and real PyTorch likelihood/serialization operations. It exercised
prefix isolation, UTF-8 byte handling,8-bit uniform scoring, suffix/batch controls, generated-prefix
feedback, manifest/protection unions and a synthetic full378-forward run with artifacts/postchecks.
These fixtures are not the actual full V5-B model or accepted historical regression.

Not executed here: test23 actual V5-B model, test24/full2689 historical suite, Windows PowerShell AST,
user-local parent artifact precheck or formal C231 execution. The reviewing container's GitHub DNS
lookup failed; connector source reads succeeded. Do not represent missing execution checks as PASS.

After all files are committed, re-fetch code/test/runner/launcher, match exact Git blob identities
against tested copies, rerun targeted tests, compile embedded Python and audit source bindings,
parent artifact semantics, exact LM source dependency coverage, union accounting, CLI wiring,
preflight-before-publication guards, no target arguments and no training. Record the review HEAD
and its execution limits before issuing a launcher command.

## Stop condition

Run24 new tests,2689 focused tests, then C231 instrument audit. Judge C231 before C232 registration.
A useful language/reasoning performance evaluation with trained models and held-out data requires a
separate registration. No large-model training budget is adopted here. No numeric-memory tuning,
cleanup, history rewrite, CI addition or Actions-storage changes are included.

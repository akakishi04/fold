# C169 preregistration — Frozen interface-readiness diagnostic batch

Registered 2026-09-17 JST following the user's explicit request to run remaining diagnostics together.
C168 acceptance precedes this registration at `c0c7e780a2c87ee36a36fed064e2396dd7c6341f`.
Read `experiment-ledger-addendum-c168-c169.md` and `gate-e-evaluation-contract-v0.1.md`.

## Formal state and question

**C169 ACTIVE / NOT YET JUDGED**.
Experiment `C169-v5e-frozen-interface-readiness-batch`.
Stage `V5-E-FROZEN-INTERFACE-READINESS-BATCH`; branch `feat/sft-target-loss`.
Use the final registration HEAD containing module/tests/runner/this document/handoff.
C168 and C160 remain ACCEPTED VALID NEGATIVE; C167 remains ACCEPTED PASS.
**Gate E NOT PASSED. No C170 registered.**

One inventory question: what information-flow, action-dispatch and output-contract limitations does the existing selected-record path expose before a bounded structured Gate E extension is implemented?
This is a source-frozen descriptive diagnostic batch, not an intervention efficacy test, new learned-model evaluation or final Gate E suite. Each section has its own raw evidence and outcome. The user-approved batching changes execution packaging, not past thresholds or verdicts. Do not turn one section's result into a mid-batch repair of another.

The accepted C168 gap is inherited explicitly, not presented as a fresh independent finding. Since no repair is allowed, an overall readiness-negative result is expected and the inherited blocker alone prevents readiness. The useful new evidence is the detailed interface map, not another claim that C168 failed. `diagnostic_execution_valid=true` plus six completed sections means the audit ran correctly; readiness is a separate field.

## Fixed scope: six sections

### D1_TASK_INPUT — accepted evidence reaggregation

Read the full hash-pinned C168 report. Recompute exact input-equivalence classes and truth-table labels; require agreement with that report. Carry the missing task/operator/known-A route into the inventory. Reuse8 challenge captures and4 source controls; do not recapture or rerun C168. Existing operator/A-only pairs remain attached.

### D2_CONTROL_LANE — what the router actually consumes

Use C168's first captured working/context/operation tuple as a fixed numeric baseline. Execute the actual `ControlLaneActionRouter.forward` feature-construction prefix with config width8/control_width4/operation_vocab_size1/hidden8/actions6.
A parameter-free proxy supplies a declared zero operation-embedding fixture for operation0; a norm callback captures the concatenated feature and aborts BEFORE normalization/linear/GELU/action-head layers. Thus the production forward METHOD prefix runs, but no learned or randomly initialized model parameters run.

19 captures:1 baseline;16 single-coordinate interventions (working0..7 and context0..7, +0.25 separately);2 balanced two-slot permutations. Record actual features and changed flags. The two-slot probe is a numeric pooling-symmetry test, not a claim that the current semantic encoder emits those slots. Four-lane truncation and slot-mean symmetry are intentional architectural boundaries, not automatically bugs. This shows why placing new task fields in unused tail positions or relying on slot order alone may not provide a usable task interface. No fields are added to the candidate.

### D3_REOBSERVATION — retained, cleared and collapsed distinctions

8 actual C156 reobservation calls:missing reference with old0/1;external dependency0 control;reference mismatch;controlled SOURCE_UNBOUND and SNAPSHOT_MISMATCH outcomes;controlled resolved0/1. Use one explicit in-memory reference with fixed request/scope/time/revision and no corpus, rather than claiming a new C167 persisted64-record trial.
Four resolver callback invocations here return typed fixture outcomes. They perform no disk/index access and their retrieval cost is0. This measures how reobservation handles those outcomes, not the correctness of the actual C155 resolver on a disk corpus.
Record old/new working tensors, status/value, canonical Controller working input, clock/budget and immutability. Check expected status/value, preserved channels0/1/4..7, one internal debit and unchanged evidence/input. Count how many canonical working inputs represent the four declared unresolved reasons. A reason may remain available to runtime while being absent from learned inputs; do not call that a lost runtime error code.
The dependency0 probe is explicitly stipulated, not learned necessity inference or an oracle repair of C168.

### D4_RUNTIME_CONTEXT — model-visible context versus enforced authority

8 actual C158 `_decision` pre-learned-call captures over availabilityFalse/True x permissionFalse/True x acquisition allowance0/1. Observation data is fixed; permission stays audit/runtime-side because `_decision` has no permission argument. No claim that varying an unused field directly tests a learned policy. Record every full tensor tuple and the outside runtime fields separately.
8 direct `_authorize` probes cross attemptedFalse/True x permissionFalse/True x allowance0/1. Require priority ATTEMPT_LIMIT, then PERMISSION_DENIED, then BUDGET_EXHAUSTED, else AUTHORIZED. No acquisition is executed. These8 direct probes are separate from the one authority call inside D5's scripted RETRIEVE cycle.

### D5_ACTION_DISPATCH — six existing action indices

Run6 tiny C158 cycles with explicitly scripted logits selecting each action index0..5. Permission isFalse and the selected reference is absent, so no fetch is allowed. Index2's script then returns5 after denial; other indices have one scripted decision. Every unexpected actual fetch/admission/publication callback raises invalid execution.
Record terminal, steps, actual authority, budget and counter state for every index. Indices routed to UNEXPECTED_ACTION are extension-handler gaps, not learned action errors. The actual dispatch code, not a guessed action-name mapping, decides which indices are supported. This does not prove that an already implemented mechanism elsewhere in FOLD is missing globally.

### D6_OUTPUT_CONTRACT — observed bits versus proposed derived results

Run2 further tiny scripted warm cycles with controlled resolved bits0/1. Use the already accepted observations-only tuple-to-list boundary representation, without modifying the historical emitter. For each bit invoke the actual C159 emitter once normally and once with only final_value contradicted against its readback.4 emissions total. Require correct observed-bit output, rejected mismatch with no value, and no input mutation.
Inspect actual `TerminalResult` dataclass fields and schema. The proposed extension would need explicit expression identity, supporting-reference collection and a derivation discriminator; check fields named expression_id/supporting_references/derivation_kind for this inventory. These names are a declared proposed extension contract, not universal field-name requirements. The inspected C159 code returns OBSERVED_VALUE, not a logical derivation. Merely adding fields would not establish a solver or correct proofs. Do not treat altered final_value as a valid derived answer, and do not weaken its rejection guard.

## Counts and unchanged variables

-6 sections collected in fixed order with no outcome-dependent interventions.
-D1 reuses12 C168 captures; no new ranking or recapture.
-19 router feature-prefix captures, before learned layers.
-8 standalone reobservation probes.
-8 pre-forward decision-input captures.
-8 direct authority probes.
-8 fixture cycles total:6 dispatch +2 observed-output controls.
-9 scripted policy calls total:7 dispatch +2 output controls.
-17 actual C156 reobservations total:8 standalone +7 dispatch +2 output.
-6 controlled resolver calls total:4 standalone +2 output.
-4 actual C159 emitter calls.
-0 learned forwards,0 checkpoints/model-parameter loads,0 real adapter calls,0 training,0 fresh seeds in the benchmark. Scripted logits and a zero embedding fixture are NOT frozen learned-model evaluations. Regression helper activity is separate.

Pin historical source to `5c181fe061e494a9ded2ccdfe6a6a94aa56321e2`, using C168's original source checker plus C168 module/tests/runner, C159 and the Gate E contract. No production code, historical evaluator, checkpoint, prior case or threshold changes. Allow only exact checked-out bytes or equivalent LF/CRLF. New source is pinned by registration HEAD and rehashed.

## Outcomes and stopping

Section `OBSERVED_BOUNDARY`: descriptive structural evidence, not a whole-Gate PASS.
Section `GAP`: missing extension interface/handler/schema, not necessarily a defect in its original scope.
Section `FAIL`: finite violation of a registered guard/transition expectation. Raw evidence stays saved.

All sections are executed before aggregation; a finite GAP/FAIL does not short-circuit independent sections. Do not average them into a misleading success rate.
Overall report status PASS would require six complete sections with no declared gaps/violations; `interface_ready=true` refers only to this inventory, never Gate E. Overall FAIL with valid execution is retained as ACCEPTED VALID NEGATIVE. Given inherited C168 and unchanged code, readiness remains negative; this is not a claim of an unseen statistical test or an invitation to rerun until PASS.

Invalid parent/hash/source, malformed probes, unexpected forbidden callback, nonfinite values, incomplete capture/accounting or outer artifact/tree/HEAD failures are INVALID. Save completed sections in invalid.json when execution reached the output directory; restore validity and retry the same C169 specification. Do not continue after global source/setup invalidity. No automatic repair or C170 execution.

## Artifacts and command

Parent C168 `runs/c168-v5e-necessity-observability-a3e8dcd5d688480687db367ec28d3a3b/summary.json`.
SHA256 `3ef1433d0f2678237f70d1dddf8b3de4783ba676fba15b607281b97f7839124c`.
Execution HEAD `5c181fe061e494a9ded2ccdfe6a6a94aa56321e2`.
No dependency on the old48 trace files or698-file recursive chain; preserve them without replaying them.
C37 and composition fixture remain outer protected files, not loaded model inputs.
Write diagnostic-plan.json before any new probe, including all section/count/source pins. New UUID run directory, full summary with every section and exact captured values, input/source/plan hashes, commit and timing. Python/PowerShell recheck protection; full result is printed without omitting records.

Runner `tools/run_c169.ps1 -C168Summary ... -ExpectedHead ...`; authoritative Python `.venv-py31315\Scripts\python.exe`.
Run focused regression ONCE: **773 expected =749 historical +24 new**, across53 modules. CPU diagnostic; no full83k-query acquisition run and no new GPU-trained model.
Progress `[C169] plan fixed`, then1/6 through6/6, `=== C169 RESULT ===`, `=== C169 POSTCHECK ===`, `run_execution_valid=True`.

## Reviewer verification and next boundary

Both new Python files compiled.16 unit tests passed in a reviewer-only harness that supplied small C168 helper substitutes; that is not a complete-repository import.8 actual-component integration tests, full773 regression, artifact-backed batch and Windows PowerShell have NOT been run here. No formal C169 deciding run was performed before registration. Source/test/runner blob hashes match the reviewed local bytes.

After this finite batch, review all findings together and make one coherent interface-change design, separating observation storage from task/derived state. Do not create another C solely to repeat known source gaps. New questions requiring a real necessity learner, expression solver, derived-output implementation, task splits or final statistical baselines belong to later explicitly registered work, not to pretend tests of nonexistent capabilities. The nine-family Gate E contract is unchanged. No promise that this inventory finds every future bug or completes all Gate E experiments.
**Judge C169 and update ledger before repairs or C170.**

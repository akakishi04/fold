# C193 preregistration — budget13 authoritative closure

**C192 ACCEPTED PASS. C193 ACTIVE / NOT YET JUDGED. C194 NOT REGISTERED.**
Gate E remains **NOT PASSED**.

## Scientific question

With exactly one changed variable — initial TaskView `internal_remaining: 12 -> 13` — does the
unchanged frozen C181+C188 / C172+C173 path remain scientifically correct over the full
coherent-world development cohort and execute an authoritative final post-third necessity
decision that terminates SUFFICIENT?

## Changed variable

Only numeric feature/resource coordinate62:
`internal_remaining 12 -> 13`.

The intervention helper must prove it changes exactly one coordinate per row.

## Held constant

- C174 cohort and split
- all9 frozen C181 base x C188 head pairs
- same16 complete coherent-world source snapshots
- same C172/C173 action/admission lifecycle
- acquisitions_remaining=4
- internal_step=7
- all other TaskView fields
- raw argmax
- scoring teachers
- no training / no fresh seeds / no production runtime modification

Because internal_remaining is model-visible, C193 does **not** force budget12 intermediate
predictions to replay. All phase0/1/2 decisions are rescored for semantic correctness under
the actual budget13 state.

## Runtime/resource path

For a path requiring all three acquisitions:

```text
start                    13 internal /4 acquisitions /step7
decision0                12 /4 /8
acquisition1 + decision1  8 /3 /12
acquisition2 + decision2  4 /2 /16
acquisition3 + decision3  0 /1 /20
```

The fourth learned decision is now an **authoritative scheduler decision**, not a shadow probe.

## Fixed gate — all 9 blocks

Per selector:
- episodes9536
- first reads9536
- necessity errors0
- target errors0
- selected observed0
- repeated target0
- acquisition errors0
- contract errors0
- final authoritative decision errors0
- third reads == final decision rows
- third/final rows >0
- 0 <= third <= second <= first
- actual reads == first + second + third
- exact resource accounting for whichever correct stopping depth is reached

All9 must pass.

A completed finite miss is **ACCEPTED VALID NEGATIVE**.
Source/hash/schema/precheck/incomplete/nonfinite infrastructure/protection failure is
**INVALID EXECUTION / RETRY SAME C193**.

## Interpretation

PASS means one extra internal unit is sufficient for the frozen learned runtime to carry its
bounded acquisition path through the authoritative post-third SUFFICIENT decision in this
development family.

PASS does not establish:
- that budget13 should become a production default;
- a learned resource policy;
- arbitrary-length looping;
- learned tool/provider choice;
- naming/permutation or independent final holdout robustness;
- answer/proof;
- Gate E completion.

## Workload

- 1768 PILOT NEEDS rows ->9536 coherent worlds/selector
- 9 selectors ->85824 episodes
- dynamic second/third read counts scored from actual valid learned path
- at least one third/final decision row required per selector
- source pins126
- protected paths342
- artifacts4 excluding summary
- expected focused regression **1557 =1533 existing +24 new**
- expected modules78
- production runtime modifiedFalse
- Gate E candidateFalse

## Authoring quality gate

Before presenting C193 as runnable:
- no parent prediction NPZ loader is used;
- coherent world sources are taken from the protected accepted C191 source directory;
- C192 summary/artifacts are lineage/protection inputs only;
- `budget13()` has unit tests proving coordinate62-only intervention and no input mutation;
- synthetic integration covers 3 acquisitions + final charged decision + resources0/1/20;
- runner parent list contains18 summaries, precheck slices17 prior summaries +root correctly,
  postcheck ExpectedHead is argv20;
- module count78 and test count1557 are fixed;
- launcher publishes C193 log remotely.

Scientific manifest SHA256:
`c038222b91af95f557a39171c18ed75e791e32455d74556c771875b9c68a95b5`.
